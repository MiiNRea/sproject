@model IEnumerable<sproject.Models.SupplierType>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.supplier_type_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.supplier_type_name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.supplier_type_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.supplier_type_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.supplier_type_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
